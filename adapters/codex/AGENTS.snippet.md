# Paste into AGENTS.md (project) or your Codex global instructions

## Skill Router

Before any task: run skill-router Phase 0 (multi-angle analysis), then route via `registry.md` (max 1 primary + 1 helper). If work is large/parallel, split into 2–4 agents and verify before claiming done. Overrides: `stop router`, `بدون موجّه`, `نفّذ مباشرة`.
